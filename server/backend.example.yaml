runtime: nodejs12
instance_class: F1
service: backend

handlers:
  - url: /assets
    static_dir: assets

automatic_scaling:
  max_instances: 1

env_variables:
  CPASS: "..."
  CUSER: "..."
  MHOST: "..."
  CORS_ORIGIN: "..."
  GITHUB_ACCESS_TOKEN: "..."
  GITHUB_API: "..."
