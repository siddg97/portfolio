runtime: nodejs12
instance_class: F1

handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
  - url: /(.*)$
    static_files: build/\1
    upload: build/(.*)

basic_scaling:
  max_instances: 1

env_variables:
  REACT_APP_SERVER_URL: "..."
